diff --git a/third_party/CMakeLists.txt b/third_party/CMakeLists.txt
index fcdca8b..6ba673c 100644
--- a/third_party/CMakeLists.txt
+++ b/third_party/CMakeLists.txt
@@ -112,7 +112,7 @@ add_library(discord-rpc STATIC
 )
 if(XE_TARGET_X86_64)
   target_compile_definitions(discord-rpc PRIVATE RAPIDJSON_SSE42)
-elseif(XE_TARGET_ARM64)
+elseif(XE_TARGET_AARCH64)
   target_compile_definitions(discord-rpc PRIVATE RAPIDJSON_NEON)
 endif()
 target_include_directories(discord-rpc PUBLIC discord-rpc/include PRIVATE rapidjson/include)
@@ -204,6 +204,13 @@ if(NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/snappy/snappy-stubs-public.h")
   endif()
   execute_process(
     COMMAND ${CMAKE_COMMAND}
+      -DCMAKE_SYSTEM_NAME=${CMAKE_SYSTEM_NAME}
+      -DCMAKE_SYSTEM_PROCESSOR=${CMAKE_SYSTEM_PROCESSOR}
+      -DCMAKE_C_COMPILER=${CMAKE_C_COMPILER}
+      -DCMAKE_C_COMPILER_TARGET=${CMAKE_C_COMPILER_TARGET}
+      -DCMAKE_CXX_COMPILER=${CMAKE_CXX_COMPILER}
+      -DCMAKE_CXX_COMPILER_TARGET=${CMAKE_CXX_COMPILER_TARGET}
+      -DCMAKE_SYSROOT=${CMAKE_SYSROOT}
       -DSNAPPY_BUILD_TESTS=OFF
       -DSNAPPY_BUILD_BENCHMARKS=OFF
       ${_snappy_avx_flag}
@@ -286,6 +293,11 @@ endforeach()
 if(_zlibng_needs_configure)
   execute_process(
     COMMAND ${CMAKE_COMMAND}
+      -DCMAKE_SYSTEM_NAME=${CMAKE_SYSTEM_NAME}
+      -DCMAKE_SYSTEM_PROCESSOR=${CMAKE_SYSTEM_PROCESSOR}
+      -DCMAKE_C_COMPILER=${CMAKE_C_COMPILER}
+      -DCMAKE_C_COMPILER_TARGET=${CMAKE_C_COMPILER_TARGET}
+      -DCMAKE_SYSROOT=${CMAKE_SYSROOT}
       -DZLIB_ENABLE_TESTS=OFF
       -DWITH_GTEST=OFF
       ${CMAKE_CURRENT_SOURCE_DIR}/zlib-ng
diff --git a/tools/build/compile_shader_spirv.py b/tools/build/compile_shader_spirv.py
index 2f7eb26..161a44e 100644
--- a/tools/build/compile_shader_spirv.py
+++ b/tools/build/compile_shader_spirv.py
@@ -79,6 +79,16 @@ def main():

     glslang, spirv_opt, spirv_dis = find_vulkan_tools()

+    # --canonicalize-ids was added in SPIRV-Tools 2025.3. Older distro
+    # packages can still generate valid, stable-enough embedded shaders with
+    # the pre-existing --compact-ids pass.
+    help_result = subprocess.run(
+        [spirv_opt, "--help"], stdout=subprocess.PIPE,
+        stderr=subprocess.STDOUT, text=True)
+    id_pass = ("--canonicalize-ids"
+               if "--canonicalize-ids" in help_result.stdout
+               else "--compact-ids")
+
     # Create output directory if needed.
     os.makedirs(os.path.dirname(output_path), exist_ok=True)

@@ -112,9 +122,9 @@ def main():

         # Step 2: spirv-opt
         result = subprocess.run([
-            spirv_opt, "-O", "-O", "--canonicalize-ids",
+            spirv_opt, "-O", "-O", id_pass,
             glslang_spv, "-o", opt_spv,
-        ], stdout=subprocess.DEVNULL, stderr=subprocess.PIPE)
+        ], stdout=subprocess.DEVNULL, stderr=subprocess.PIPE, text=True)
         if result.returncode != 0:
             print(f"ERROR: spirv-opt failed for {src_name}", file=sys.stderr)
             if result.stderr:
@@ -123,7 +133,8 @@ def main():

         # Step 3: spirv-dis
         result = subprocess.run([spirv_dis, "-o", dis_txt, opt_spv],
-                                stdout=subprocess.DEVNULL, stderr=subprocess.PIPE)
+                                stdout=subprocess.DEVNULL, stderr=subprocess.PIPE,
+                                text=True)
         if result.returncode != 0:
             print(f"ERROR: spirv-dis failed for {src_name}", file=sys.stderr)
             if result.stderr:
diff --git a/xenia-build.py b/xenia-build.py
index 8000367..29beadf 100755
--- a/xenia-build.py
+++ b/xenia-build.py
@@ -703,10 +703,10 @@ def normalize_target_arch(value):
         f"unknown architecture '{value}' (expected: arm64, aarch64, a64, x64, amd64, x86_64, x86)")

 def is_amd64():
-    return normalize_target_arch(platform.machine()) in ("x64", "x86_64", "amd64", "x86")
+    return normalize_target_arch(platform.machine()) == "x64"

 def is_arm():
-    return normalize_target_arch(platform.machine()) in ("x64", "x86_64", "amd64", "x86")
+    return normalize_target_arch(platform.machine()) == "arm64"

 def get_build_dir(target_arch=None):
     """Returns the Ninja build directory for the given target architecture.
