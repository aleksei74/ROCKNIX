diff --git a/CMakeLists.txt b/CMakeLists.txt
index 0db36ca80..3baff5e25 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -357,12 +357,18 @@ else()
     )
   elseif(CMAKE_SYSTEM_NAME STREQUAL "Linux")
     find_package(PkgConfig REQUIRED)
-    pkg_check_modules(GTK3 REQUIRED gtk+-x11-3.0)
-    include_directories(${GTK3_INCLUDE_DIRS})
-    link_directories(${GTK3_LIBRARY_DIRS})
-    add_compile_options(${GTK3_CFLAGS_OTHER})
+    if(NOT BUILD_HOST_TOOLS_ONLY)
+      pkg_check_modules(GTK3 REQUIRED gtk+-x11-3.0)
+      include_directories(${GTK3_INCLUDE_DIRS})
+      link_directories(${GTK3_LIBRARY_DIRS})
+      add_compile_options(${GTK3_CFLAGS_OTHER})
+    endif()
 
-    link_libraries(stdc++fs dl lz4 m pthread rt)
+    if(NOT BUILD_HOST_TOOLS_ONLY)
+      link_libraries(stdc++fs dl lz4 m pthread rt)
+    else()
+      link_libraries(stdc++fs dl m pthread rt)
+    endif()
 
     # Link group so executables resolve circular static-lib deps.
     set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -Wl,--start-group")
@@ -446,6 +452,7 @@ endif()
 
 add_subdirectory(third_party)
 
-add_subdirectory(src/xenia)
-
-set_property(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT xenia-app)
+if(NOT BUILD_HOST_TOOLS_ONLY)
+  add_subdirectory(src/xenia)
+  set_property(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT xenia-app)
+endif()
diff --git a/third_party/CMakeLists.txt b/third_party/CMakeLists.txt
index d2d4fe824..d4fc96518 100644
--- a/third_party/CMakeLists.txt
+++ b/third_party/CMakeLists.txt
@@ -188,8 +188,7 @@ target_include_directories(glslang-spirv PUBLIC glslang)
 # (Windows-on-ARM runs x64 fine via Prism, and macOS handles its own
 # x86_64↔arm64 via Rosetta — both directions are unaffected).
 set(_xe_use_host_shader_cc FALSE)
-if(WIN32 AND CMAKE_CROSSCOMPILING AND XE_TARGET_AARCH64
-    AND CMAKE_HOST_SYSTEM_PROCESSOR MATCHES "AMD64|x86_64")
+if(CMAKE_CROSSCOMPILING AND XE_TARGET_AARCH64)
   set(_xe_use_host_shader_cc TRUE)
 endif()
 
@@ -430,12 +429,21 @@ endif()
 else()  # _xe_use_host_shader_cc: x64→ARM64 cross-compile, use native build's binary
 
 # Native x64 build lives at <repo>/build/ (xenia-build.py: get_build_dir(None)).
-set(_xe_host_shader_cc_dir "${CMAKE_SOURCE_DIR}/build/host_tools")
+set(_xe_host_shader_cc_dir "${CMAKE_SOURCE_DIR}/build-host/host_tools")
 set(_xe_host_shader_cc_any "")
-foreach(_cfg Release Checked Debug)
+if(EXISTS "${_xe_host_shader_cc_dir}/xenia-shader-cc")
+  set(_xe_host_shader_cc_any "${_xe_host_shader_cc_dir}/xenia-shader-cc")
+endif()
+foreach(_cfg Release Checked Debug .)
   if(EXISTS "${_xe_host_shader_cc_dir}/${_cfg}/xenia-shader-cc.exe")
     set(_xe_host_shader_cc_any "${_xe_host_shader_cc_dir}/${_cfg}/xenia-shader-cc.exe")
     break()
+  elseif(EXISTS "${_xe_host_shader_cc_dir}/${_cfg}/xenia-shader-cc")
+    set(_xe_host_shader_cc_any "${_xe_host_shader_cc_dir}/${_cfg}/xenia-shader-cc")
+    break()
+  elseif(EXISTS "${CMAKE_SOURCE_DIR}/build/host_tools/${_cfg}/xenia-shader-cc.exe")
+    set(_xe_host_shader_cc_any "${CMAKE_SOURCE_DIR}/build/host_tools/${_cfg}/xenia-shader-cc.exe")
+    break()
   endif()
 endforeach()
 if(NOT _xe_host_shader_cc_any)
@@ -455,6 +463,10 @@ set_target_properties(xenia-shader-cc PROPERTIES
 
 endif()
 
+if(BUILD_HOST_TOOLS_ONLY)
+  return()
+endif()
+
 # ==============================================================================
 # imgui
 # ==============================================================================
