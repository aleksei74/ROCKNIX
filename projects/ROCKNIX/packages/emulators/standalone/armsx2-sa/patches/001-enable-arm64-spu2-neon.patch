--- a/pcsx2/CMakeLists.txt
+++ b/pcsx2/CMakeLists.txt
@@ -1116,8 +1116,10 @@ if(CMAKE_SYSTEM_NAME STREQUAL "iOS")
 	set(ARMSX2_IOS TRUE)
 endif()
 
-# NEON-optimised SPU2 mixer/reverb: shared by all arm64 mobile targets.
-if(ANDROID OR ARMSX2_IOS)
+# spu2.cpp calls RegisterNEONBackend() on every ARM64 target. Keep the backend
+# source linked on desktop Linux ARM64 as well, even though it remains opt-in
+# through the NeonReverbSIMD setting.
+if(ANDROID OR ARMSX2_IOS OR ARCH_ARM64)
 	list(APPEND pcsx2SPU2Sources
 		SPU2/spu2_neon.cpp)
 	list(APPEND pcsx2SPU2Headers
